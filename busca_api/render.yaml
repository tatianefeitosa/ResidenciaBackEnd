services:
  - type: web
    name: busca-api
    env: python
    branch: main
    buildCommand: pip install -r requirements.txt
    startCommand: bash -lc "python manage.py migrate --noinput && python manage.py collectstatic --noinput && gunicorn busca_api.wsgi:application --bind 0.0.0.0:$PORT"
